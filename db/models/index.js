const {
    List
} = require('./list.model');

const {
    Task
} = require('./task.model');

const {
    User
} = require('./user.model');

const {
    IpAddress
} = require('./ip-address.model');


module.exports = {
    List,
    Task,
    User,
    IpAddress
}